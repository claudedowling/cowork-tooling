# Secrets and machine-local config: NEVER commit
.credentials/
.op-env
config.sh
*_token
*.pem
id_*
signing-key*
allowed_signers

# Large third-party binaries (download separately; see README)
bin/gh-real
bin/op

# Runtime artifacts written by session-init (not source)
bin/gitleaks
bin/*.new
.githooks-template/

# Ephemeral repo checkouts
repos/
