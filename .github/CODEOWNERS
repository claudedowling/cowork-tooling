# Require review on the pieces that guard main's trust boundary, so a PR
# can't quietly weaken the checks that review it or the scripts they protect.
# Only takes effect once "Require review from Code Owners" is turned on in
# branch protection for main (admin-only setting); tracked in issue #3.

/bin/          @stevedowling
/.github/      @stevedowling
/.semgrep/     @stevedowling
/.shellcheckrc @stevedowling
